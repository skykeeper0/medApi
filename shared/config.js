module.exports = () => {
  const host = 'localhost';
  const port = '5432';
  const database = 'mydb';
  const username = 'skykeeper0';
  return {
    host,
    port,
    database,
    username
  };
};